package com.happizo.account.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class JacksonConfig {
    // No special Jackson config needed — all entities are simple POJOs with no lazy loading
}
